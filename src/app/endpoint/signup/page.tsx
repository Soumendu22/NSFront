import { EndpointSignupForm } from '@/components/EndpointComp/EndpointSignupForm';

export default function EndpointSignupPage() {
  return <EndpointSignupForm />;
}

export const metadata = {
  title: 'Request Access - NexusSentinel',
  description: 'Request access to join an organization\'s NexusSentinel security network',
};
